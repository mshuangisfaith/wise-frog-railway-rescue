'use strict';
(() => {
 const E=window.RailwayEngine,P=window.RailwayPacks,app=document.querySelector('#app');
 const KEY='wise-frog-railway-v1';
 let state={screen:'start',hero:'frog',mode:'connectors',index:0,lives:3,streak:0,seconds:0,records:[],order:[],answer:null,proof:null,feedback:null,muted:true};
 let saved=null,storageOK=true;
 try{const s=JSON.parse(localStorage.getItem(KEY));if(s&&s.version===1&&['game','finish'].includes(s.state?.screen)&&P[s.state.mode]&&Array.isArray(s.state.records)&&s.state.records.length===P[s.state.mode].length&&Number.isInteger(s.state.index)&&s.state.index>=0&&s.state.index<P[s.state.mode].length)saved=s.state;}catch{storageOK=false;}
 const audio=new Audio('./trail-music.mp3');audio.loop=true;audio.volume=.18;
 let tickAt=Date.now();
 const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const cap=s=>s.charAt(0).toUpperCase()+s.slice(1);
 const low=s=>/^(Zak|I)\b/.test(s)?s:s.charAt(0).toLowerCase()+s.slice(1);
 const clock=()=>`${String(Math.floor(state.seconds/60)).padStart(2,'0')}:${String(state.seconds%60).padStart(2,'0')}`;
 const pack=()=>P[state.mode],q=()=>pack()[state.index],record=()=>state.records[state.index];
 function save(){try{localStorage.setItem(KEY,JSON.stringify({version:1,state}));}catch{storageOK=false;}}
 function play(){if(!state.muted)audio.play().catch(()=>{state.muted=true;render();});else audio.pause();}
 function hero(kind,cls=''){
 return `<svg class="hero ${cls}" viewBox="0 0 150 150" aria-hidden="true"><ellipse cx="75" cy="139" rx="43" ry="7" fill="#345941" opacity=".13"/>${kind==='frog'?`
 <g stroke="#365535" stroke-width="3" stroke-linejoin="round"><ellipse cx="76" cy="107" rx="31" ry="33" fill="#8baf51"/><ellipse cx="75" cy="111" rx="21" ry="23" fill="#d6df93"/><path d="M51 124q-38 15-21 16h32m36-16q35 15 18 16H87" fill="#83a94a"/><path d="M47 98l-17 24m73-24 17 24" fill="none" stroke-linecap="round" stroke-width="9"/><ellipse cx="75" cy="68" rx="47" ry="31" fill="#91b856"/><circle cx="48" cy="43" r="21" fill="#91b856"/><circle cx="102" cy="43" r="21" fill="#91b856"/></g><g fill="#fff9d7"><ellipse cx="48" cy="42" rx="13" ry="15"/><ellipse cx="102" cy="42" rx="13" ry="15"/></g><g fill="#293e2c"><ellipse cx="51" cy="44" rx="5" ry="8"/><ellipse cx="99" cy="44" rx="5" ry="8"/></g><g fill="#dc8b72" opacity=".7"><ellipse cx="41" cy="71" rx="9" ry="5"/><ellipse cx="109" cy="71" rx="9" ry="5"/></g><path d="M55 74q20 20 40 0" fill="none" stroke="#365535" stroke-width="3" stroke-linecap="round"/><path d="M51 93l23 11 25-11-8 20-18-8-17 8z" fill="#d8804d"/>`:`
 <path d="M40 87q-10-62 34-61 45-1 36 61l-9 22H49z" fill="#684936" stroke="#503d2e" stroke-width="3"/><path d="M64 122v16m24-16v16" stroke="#eeb990" stroke-width="10"/><path d="M48 94L34 113m67-19 14 19" stroke="#efba91" stroke-width="10" stroke-linecap="round"/><path d="M55 87h39l18 45q-36 11-74 0z" fill="#d37b70" stroke="#8d514b" stroke-width="3"/><path d="M54 119q20-12 46 0" fill="none" stroke="#f1bdaa" stroke-width="4"/><ellipse cx="75" cy="63" rx="31" ry="33" fill="#f0c59c" stroke="#84533e" stroke-width="2"/><path d="M42 54q-2-30 34-26 30-4 34 27-25 0-36-14-13 15-32 13" fill="#684936"/><g fill="#463b33"><circle cx="63" cy="65" r="3"/><circle cx="88" cy="65" r="3"/></g><g fill="#df977d"><ellipse cx="54" cy="75" rx="7" ry="4"/><ellipse cx="97" cy="75" rx="7" ry="4"/></g><path d="M68 78q8 7 15 0" fill="none" stroke="#935949" stroke-width="2" stroke-linecap="round"/><path d="M54 31l-3-21 15 9 10-16 10 16 15-9-3 21z" fill="#e6b743" stroke="#a87928" stroke-width="2"/><circle cx="76" cy="23" r="4" fill="#d47765"/><path d="M61 90l14 10 14-10" fill="#fff5d8"/><circle cx="75" cy="104" r="4" fill="#e6b743"/>`}</svg>`;
 }
 function scene(){
 const total=pack().length,progress=state.screen==='start'?12:(state.screen==='finish'?100:(state.index+(record()?.completed?1:0))/total*100);
 return `<div class="landscape" role="img" aria-label="A handmade railway through a palace garden and frog pond"><svg class="scenery" viewBox="0 0 1200 330" preserveAspectRatio="xMidYMid slice" aria-hidden="true"><defs><pattern id="paperlines" width="9" height="9" patternUnits="userSpaceOnUse"><path d="M0 9L9 0" stroke="#fff" opacity=".08"/></pattern><pattern id="sleepers" width="30" height="40" patternUnits="userSpaceOnUse"><path d="M10 0v40" stroke="#876745" stroke-width="12"/></pattern></defs><rect width="1200" height="330" fill="#dbeae1"/><circle cx="978" cy="65" r="38" fill="#f0cf71"/><g fill="#faf7e8"><path d="M124 61q-3-30 27-30 14-22 39-6 34-11 42 20 23-2 25 16z"/><path d="M650 53q0-19 23-19 12-29 37-10 32-10 35 17 20-1 23 12z"/></g><path d="M-30 232Q146 45 363 216T744 203T1240 211V350H-30" fill="#b6ca90"/><path d="M-30 263Q151 124 351 250T741 240T1240 243V350H-30" fill="#8bab78"/><g transform="translate(110 75)" stroke="#988d71" stroke-width="2"><path d="M0 150V57h33v93m4 0V27h67v123m6 0V57h33v93" fill="#ece3c9"/><path d="M-8 57l25-34 25 34m-12-30 41-42 40 42m-11 30 25-34 25 34" fill="#be7766"/><path d="M59 150v-32q12-25 24 0v32" fill="#9c8d67"/><path d="M14 86v15m110-15v15M60 55v16m22-16v16" stroke="#73867d" stroke-width="9"/><path d="M70-15v-24h30l-7 8 7 7H70" fill="#dbad4b"/></g><g transform="translate(845 144)"><ellipse cy="106" rx="158" ry="39" fill="#ddd6ae"/><ellipse cy="103" rx="147" ry="30" fill="#93bec0"/><g fill="#6f9759"><ellipse cx="-56" cy="99" rx="24" ry="8"/><ellipse cx="45" cy="112" rx="19" ry="7"/></g><path d="M-89 100h40m60-6h51m-23 28h35" stroke="#d2e4cf" stroke-width="3"/><g stroke="#58774c" stroke-width="4"><path d="M-125 100v-47m9 48V65m240 39V51"/></g><g fill="#946b45"><rect x="-129" y="44" width="8" height="21" rx="4"/><rect x="-120" y="57" width="8" height="18" rx="4"/><rect x="120" y="43" width="8" height="22" rx="4"/></g></g><g transform="translate(1080 162)"><path d="M0 110V26" stroke="#816345" stroke-width="13"/><path d="M-52 56q-6-45 22-40-4-44 34-40 37-3 30 38 41 8 23 43z" fill="#63865b"/></g><path d="M0 281Q330 270 570 286T1200 282v48H0" fill="#c7b890"/><rect y="280" width="1200" height="30" fill="url(#sleepers)"/><path d="M0 286H1200M0 307H1200" stroke="#514e40" stroke-width="5"/><path d="M0 283H1200M0 304H1200" stroke="#e6dcc4" stroke-width="2"/><rect width="1200" height="330" fill="url(#paperlines)"/><g fill="#f8ead1"><circle cx="328" cy="257" r="5"/><circle cx="350" cy="265" r="4"/><circle cx="992" cy="269" r="5"/></g></svg><div class="station-label">${state.mode==='inference'?'02 · CLUE CARRIAGES':'01 · PALACE GARDEN LINE'}</div><div class="train" style="left:${8+progress*.78}%">${hero(state.hero,'train-hero')}<div class="carriage"><span>✦</span><i></i><i></i></div></div><div class="finish-sign">FINISH<span>⚑</span></div></div>`;
 }
 function header(){return `<header class="topbar"><a class="brand" href="./index.html" aria-label="Railway Rescue home"><img src="icon.svg" alt="" width="35" height="35"><span>WISE FROG<span class="brand-sub">RAILWAY RESCUE</span></span></a><div class="top-actions">${state.screen!=='start'?`<span class="time" aria-label="Time spent"><span aria-hidden="true">◷</span> <span id="timer">${clock()}</span></span>`:''}<button class="quiet small" data-action="sound" aria-pressed="${!state.muted}">${state.muted?'♫ Sound off':'♫ Sound on'}</button><button class="quiet small" data-action="teacher">Teacher notes</button></div></header>`;}
 function start(){return `<div class="start-layout"><section class="welcome"><p class="eyebrow">PRIMARY 3 · A STORYBOOK ADVENTURE</p><h1>A little wisdom.<br>A big <em>adventure.</em></h1><p class="lead">The railway needs a helping hand! Build sentences, follow clues and bring the garden train home.</p><div class="ticket"><span class="ticket-icon">⌁</span><div><b>YOUR GARDEN RAIL PASS</b><p>Choose a buddy. Pick a route. Let’s roll!</p></div><span class="ticket-stamp">P3<br>U8</span></div><p class="source-note">Inspired by <i>Prince Zak and the Wise Frog</i>.<br>The railway is our own make-believe adventure.</p></section><section class="boarding paper"><p class="eyebrow">ALL ABOARD</p><h2>Who’s coming along?</h2><div class="hero-options">${['frog','princess'].map(k=>`<button class="hero-choice ${state.hero===k?'selected':''}" data-hero="${k}" aria-pressed="${state.hero===k}">${hero(k)}<strong>${k==='frog'?'Wise Frog':'Princess'}</strong><small>${k==='frog'?'A thoughtful little helper':'A curious adventurer'}</small><span class="selection-dot">${state.hero===k?'✓':'○'}</span></button>`).join('')}</div><h3>Choose your route</h3><div class="route-options">${[['connectors','01','Railway Rescue','12 sentence puzzles · if, as, while'],['inference','02','Clue Carriages','8 reading puzzles · clues & evidence']].map(([id,n,title,sub])=>`<button class="route ${state.mode===id?'selected':''}" data-mode="${id}" aria-pressed="${state.mode===id}"><span class="route-number">${n}</span><span><b>${title}</b><small>${sub}</small></span><span class="route-radio">${state.mode===id?'●':'○'}</span></button>`).join('')}</div><button class="primary wide" data-action="start">Let’s go! <span>→</span></button>${saved?`<button class="quiet wide resume" data-action="resume">Continue saved ${saved.mode==='connectors'?'sentence':'reading'} journey →</button>`:''}<p class="boarding-note">3 lives · a bonus life for 3 independent answers in a row<br>No race. Take your time to think.</p></section></div>`;}
 function sentence(item,order){const chunks={W:item.word,A:item.a,B:item.b};if(order.length===3&&order[0]==='W')return `${cap(item.word)} ${low(chunks[order[1]])}, ${low(chunks[order[2]])}.`;return order.map((id,i)=>i===0?cap(chunks[id]):low(chunks[id])).join(' ')+(order.length===3?'.':'');}
 function modelText(item){const a=low(item.a),b=low(item.b);return `${cap(item.word)} ${a}, ${b}.`;}
 function progress(){let n=state.index+(record().completed?1:0);return `<div class="journey"><div><b>${state.mode==='connectors'?'Railway Rescue':'Clue Carriages'}</b><span>${n} of ${pack().length} stops repaired</span></div><div class="progress-track" role="progressbar" aria-label="Stops repaired" aria-valuenow="${n}" aria-valuemin="0" aria-valuemax="${pack().length}"><span style="width:${n/pack().length*100}%"></span></div></div>`;}
 function game(){const item=q(),r=record(),done=r.completed;
 return `<div class="play-layout"><aside class="travel-card paper"><div class="buddy-medallion">${hero(state.hero)}</div><p class="eyebrow">YOUR TRAVEL BUDDY</p><h2>${state.hero==='frog'?'Wise Frog':'Princess'}</h2><p>“${state.lives===0?'We can keep learning together!':state.mode==='connectors'?'Let’s connect these ideas.':'What clues can we find?'}”</p><div class="life-box"><span>Lives</span><strong aria-label="${state.lives} lives">♥ ${state.lives}</strong></div><div class="streak-box"><span>Independent streak</span><div aria-label="${state.streak} out of 3">${[0,1,2].map(i=>`<i class="${i<state.streak?'lit':''}">★</i>`).join('')}</div><small>3 in a row earns an extra life.</small></div><div class="tip-card"><b>${state.lives===0?'PRACTICE MODE':'LITTLE REMINDER'}</b><p>${state.lives===0?'You can still finish every stop. Use a hint, learn, and try again.':'A wrong answer is a chance to learn. Read the feedback and have another go.'}</p></div><button class="quiet wide" data-action="pause">Save & take a break</button></aside><section class="question paper">${progress()}<div class="question-heading"><span class="eyebrow">STOP ${String(state.index+1).padStart(2,'0')} / ${pack().length}</span><span class="skill-badge">${item.type==='connector'?`Joining word: ${item.word}`:'Read · infer · explain'}</span></div>${item.type==='connector'?connectorUI(item,done):inferenceUI(item,done)}${r.hint&&!done?`<div class="hint-panel"><b>Buddy hint</b><p>${item.type==='connector'?(item.word==='if'?'Find the condition. Put “if” directly before the condition, then include the other idea.':'These events happen at the same time. Put the joining word before a clause, at the start or between the two ideas.'):'Choose what the clue suggests. Then pick the detail that actually supports that idea, not just another true fact.'}</p></div>`:''}${state.feedback?`<div id="feedback" class="feedback ${state.feedback.correct?'correct':'wrong'}" tabindex="-1" role="status"><strong>${state.feedback.title}</strong><p>${esc(state.feedback.text)}</p>${r.model?`<p class="model-answer"><b>Let’s learn:</b> ${esc(item.type==='connector'?modelText(item):`${item.options[item.answer]} Evidence: ${item.evidence[item.proof]}`)}</p>`:''}</div>`:''}<div class="question-actions">${done?`<button class="primary" data-action="next">${state.index===pack().length-1?'Cross the finish line':'Next stop'} <span>→</span></button>`:`<button class="primary" data-action="check" ${item.type==='connector'?state.order.length!==3?'disabled':'':state.answer===null||state.proof===null?'disabled':''}>Check my ${item.type==='connector'?'sentence':'thinking'} <span>✓</span></button><button class="quiet" data-action="hint" ${r.hint?'disabled':''}>${r.hint?'Hint used':'✦ Buddy hint'}</button>${r.attempts.length?'<button class="quiet" data-action="model">Learn with the answer</button>':''}`}</div><p class="assessment-note">${done?'Your first answer has been kept for your teacher.':'Hints help you learn. Your teacher can see which answers needed support.'}</p></section></div>`;
 }
 function connectorUI(item,done){const chunks={A:item.a,B:item.b,W:item.word};const order=['B','W','A'];return `<h1 class="question-title">Join the ideas using <em>${item.word}</em>.</h1><p class="instructions">Tap the pieces in order to build one sentence.</p><div class="original-ideas"><p><span>A</span>${esc(cap(item.a))}.</p><p><span>B</span>${esc(cap(item.b))}.</p></div><div class="answer-label"><b>Your sentence</b>${!done?'<button class="text-button" data-action="clear">Clear pieces</button>':''}</div><div class="sentence-builder ${done?'locked':''}" aria-label="Your sentence"><div class="placed-pieces">${state.order.length?state.order.map((id,i)=>`<button class="piece ${id==='W'?'connector-piece':''}" data-remove="${i}" ${done?'disabled':''} aria-label="Remove ${esc(chunks[id])}">${esc(i===0?cap(chunks[id]):low(chunks[id]))}${!done?'<span aria-hidden="true">×</span>':''}</button>`).join(''):'<span class="placeholder">Your sentence goes here…</span>'}</div>${state.order.length===3?`<p class="sentence-readback">${esc(sentence(item,state.order))}</p>`:''}</div>${!done?`<div class="piece-pool" aria-label="Available sentence pieces">${order.map(id=>`<button class="piece ${id==='W'?'connector-piece':''}" data-piece="${id}" ${state.order.includes(id)?'disabled':''}>${esc(chunks[id])}<span aria-hidden="true">+</span></button>`).join('')}</div><p class="small-note">Tap a placed piece to take it out. Both ideas must stay in the sentence.</p>`:''}`;}
 function inferenceUI(item,done){return `<h1 class="question-title">${esc(item.prompt)}</h1><div class="reading-clue"><span class="eyebrow">STORY CLUE · PARAPHRASED</span><p>${esc(item.clue)}</p><small>${item.page}</small></div>${[['answer','1. What can you infer?',item.options,state.answer],['proof','2. Which detail supports it?',item.evidence,state.proof]].map(([key,label,opts,value])=>`<fieldset class="answers" ${done?'disabled':''}><legend>${label}</legend>${opts.map((o,i)=>`<label class="answer-option ${value===i?'chosen':''}"><input type="radio" name="${key}" value="${i}" ${value===i?'checked':''}><span class="letter">${'ABC'[i]}</span><span>${esc(o)}</span></label>`).join('')}</fieldset>`).join('')}`;}
 function resultLabel(r){if(r.model)return 'Model answer used';if(r.attempts[0]?.correct&&r.attempts[0]?.independent)return 'Independent';if(r.attempts[0]?.correct)return 'Correct with hint';if(r.completed)return 'Correct after retry';return 'Not finished';}
 function answerReview(item,r){
  const review=E.review(item,r),first=review.first;
  const firstAnswer=!first?'No answer was submitted.':item.type==='connector'?sentence(item,first.response):item.options[first.response.answer];
  const expected=item.type==='connector'?modelText(item):item.options[item.answer];
  return `<details class="item-review"><summary>${esc(r.id)} · ${esc(r.skill)} <span>${resultLabel(r)}</span></summary><div class="review-body"><p class="review-question">${esc(item.type==='connector'?`Join with ${item.word}: ${cap(item.a)}. ${cap(item.b)}.`:item.prompt)}</p><p class="review-support">First response: ${esc(review.support)} · ${r.attempts.length} ${r.attempts.length===1?'try':'tries'} · Hint: ${r.hint?'yes':'no'} · Model: ${r.model?'yes':'no'}</p><div class="review-pair"><div><h3>${item.type==='connector'?'First sentence':'First inference'}${first?` — ${item.type==='connector'?(first.correct?'correct':'needs work'):(first.inference?'correct':'needs work')}`:''}</h3><p>${esc(firstAnswer)}</p></div><div><h3>${item.type==='connector'?'One correct sentence':'Supported inference'}</h3><p>${esc(expected)}</p></div></div>${item.type==='inference'?`<div class="review-pair"><div><h3>First evidence choice${first?` — ${first.evidence?'correct':'needs work'}`:''}</h3><p>${esc(first?item.evidence[first.response.proof]:'No evidence was submitted.')}</p></div><div><h3>Supporting evidence</h3><p>${esc(item.evidence[item.proof])}</p></div></div>`:''}<p><b>Why it works:</b> ${esc(item.explanation)}</p><p class="review-next"><b>Discuss next:</b> ${esc(review.focus)}</p></div></details>`;
 }
 function finish(){const stats=E.stats(state.records);return `<section class="results paper"><div class="finish-ribbon">JOURNEY COMPLETE</div>${hero(state.hero,'finish-hero')}<p class="eyebrow">THE GARDEN LINE IS OPEN!</p><h1>Hooray,<br><em>you did it!</em></h1><p>You repaired all ${pack().length} stops. A little thinking goes a long way.</p><div class="result-stats"><div><strong>${stats.independent}<small> / ${pack().length}</small></strong><span>Independent answers</span></div><div><strong>${clock()}</strong><span>Time spent · no speed marks</span></div><div><strong>${state.lives} ♥</strong><span>Lives left</span></div></div><div class="teacher-results"><h2>Your learning journey</h2><p>Show this screen to your teacher. Independent means correct on the first try, before any hint or model answer.</p><div class="skill-results">${[...new Set(pack().map(q=>q.skill))].map(skill=>{const rows=state.records.filter(r=>r.skill===skill);return `<div><b>${esc(skill)}</b><span>${E.stats(rows).independent} / ${rows.length} independent</span></div>`;}).join('')}</div>${state.mode==='inference'?`<p class="evidence-summary">First responses: <b>${state.records.filter(r=>r.attempts[0]?.inference).length}/8</b> inferences and <b>${state.records.filter(r=>r.attempts[0]?.evidence).length}/8</b> evidence choices correct. These counts include answers after hints.</p>`:''}<details><summary>See all answers and support used</summary><p>Open a stop to compare the original response with a correct answer. Retries do not replace the first response.</p><div class="answer-reviews">${state.records.map((r,i)=>answerReview(pack()[i],r)).join('')}</div></details><p class="small-note">${stats.hints} ${stats.hints===1?'hint':'hints'} used · ${stats.retries} ${stats.retries===1?'stop':'stops'} retried · ${stats.models} model ${stats.models===1?'answer':'answers'} used. Results stay on this device; they are not sent to a teacher automatically.</p></div><div class="finish-actions"><button class="primary" data-action="home">Choose another journey →</button><button class="quiet" data-action="export">Download teacher results</button><button class="quiet" data-action="print">Print results</button></div><p id="export-status" role="status"></p></section>`;}
 function teacher(){const d=document.querySelector('#teacher');d.showModal();}
 function modal(){return `<dialog id="teacher" aria-labelledby="teacher-title"><div class="dialog-heading"><h2 id="teacher-title">Teacher notes</h2><button class="quiet" data-action="close-teacher" aria-label="Close teacher notes">Close ×</button></div><p><b>Railway Rescue:</b> 12 tap-to-order items on conditional <i>if</i> and simultaneous/developing actions with <i>as/while</i>. The given connector must stay with a complete clause. Both clause orders are accepted where meaning is preserved. Reversing the if-condition is not accepted.</p><p><b>Clue Carriages:</b> 8 inference-and-evidence pairs based on the reader. Read the story together first. Ask pupils to explain one answer aloud: selecting an option alone does not demonstrate independent reasoning.</p><p><b>Assessment:</b> first responses, retries, hints and model answers are stored separately. Only a first correct answer without support counts towards the three-answer life reward. Zero lives enables continued practice.</p><p><b>Classroom use:</b> each pupil shows the results screen or downloads a CSV. This game does not submit results online. Only the latest journey is saved in this browser. Export results before starting a new journey on a shared iPad.</p><p><b>Unit alignment:</b> guidelines pp. 18–20; reader pp. 1–9. The railway scenarios and artwork are original. The princess is a playable adventure character inspired by the companion story.</p><p><b>Controls:</b> tap or use Tab and Enter/Space. Music is off until selected. The timer pauses when the page is hidden. “Save & take a break” preserves the current journey.</p><button class="primary" data-action="close-teacher">Ready to play →</button></dialog>`;}
 function render(focus){app.innerHTML=header()+scene()+(state.screen==='start'?start():state.screen==='game'?game():finish())+`<footer><span>MADE FOR LITTLE THINKERS</span><span>Prince Zak · Primary 3 English</span></footer>`+modal();if(!storageOK){const p=document.createElement('p');p.className='storage-note';p.textContent='This browser cannot save progress. Keep this page open and download results before leaving.';app.appendChild(p);}if(focus){const el=document.querySelector(focus);if(el){el.setAttribute('tabindex','-1');el.focus({preventScroll:true});el.scrollIntoView({block:'nearest',behavior:'instant'});}}}
 function celebrate(){if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;const el=document.querySelector('#celebration');el.innerHTML=Array.from({length:24},(_,i)=>`<i style="--x:${(i*37)%100}%;--delay:${(i%5)*.06}s;--colour:${['#d59a48','#799850','#ce7f70','#84a8a5'][i%4]};--turn:${i*37}deg"></i>`).join('');setTimeout(()=>el.innerHTML='',1700);}
 function newGame(){state.screen='game';state.index=0;state.lives=3;state.streak=0;state.seconds=0;state.records=pack().map(E.createRecord);state.order=[];state.answer=null;state.proof=null;state.feedback=null;saved=null;tickAt=Date.now();play();save();render('.question-title');}
 function check(){const item=q(),r=record();if(r.completed||(item.type==='connector'?state.order.length!==3:state.answer===null||state.proof===null))return;const result=E.submit(state,item,r,item.type==='connector'?state.order:{answer:state.answer,proof:state.proof});let title,text;
 if(result.correct){title=result.bonus?'Correct! Three in a row — extra life!':'Correct! Track repaired.';text=item.explanation;celebrate();}
 else {title='Not quite — let’s think again.';if(item.type==='connector'){text=item.word==='if'?'The meaning has changed. “If” must go directly before the condition in idea A. Try moving the pieces.':'The joining word needs a whole idea after it. Place it at the start or between the two ideas, not at the end.';}else text=result.inference?'Your inference works. Now choose the detail that supports that particular idea.':result.evidence?'You found the useful evidence. Look again at what it tells you about the character.':'Check both choices against the story clue. Avoid adding information that the clue does not support.';}
 state.feedback={...result,title,text};save();render('#feedback');}
 function exportCSV(){let rows=[['Route','Character','Stop','Skill','Attempt','Response','Correct','Inference correct','Evidence correct','Independent attempt','Hint used','Model used','Result','Time seconds']];state.records.forEach((r,i)=>{const item=pack()[i];const tries=r.attempts.length?r.attempts:[null];tries.forEach((a,j)=>{let response=a?(item.type==='connector'?sentence(item,a.response):`Inference: ${item.options[a.response.answer]} | Evidence: ${item.evidence[a.response.proof]}`):'';rows.push([state.mode,state.hero,r.id,r.skill,a?j+1:0,response,a?.correct??'',a?.inference??'',a?.evidence??'',a?.independent??'',r.hint,r.model,resultLabel(r),state.seconds]);});});const csv='\uFEFF'+rows.map(row=>row.map(v=>'"'+String(v).replace(/"/g,'""')+'"').join(',')).join('\r\n');const blob=new Blob([csv],{type:'text/csv;charset=utf-8'});const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`wise-frog-${state.mode}-${new Date().toISOString().slice(0,10)}.csv`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),30000);document.querySelector('#export-status').textContent='Results download requested. On iPad, check your browser’s Downloads.';}
 app.addEventListener('change',e=>{if(e.target.matches('input[type=radio]')){state[e.target.name]=Number(e.target.value);state.feedback=null;document.querySelector('#feedback')?.remove();document.querySelectorAll(`input[name="${e.target.name}"]`).forEach(input=>input.closest('label').classList.toggle('chosen',input.checked));document.querySelector('[data-action="check"]').disabled=state.answer===null||state.proof===null;save();}});
 app.addEventListener('click',e=>{const b=e.target.closest('button');if(!b||b.disabled)return;
 if(b.dataset.hero){state.hero=b.dataset.hero;render();document.querySelector(`[data-hero="${state.hero}"]`).focus({preventScroll:true});return;}
 if(b.dataset.mode){state.mode=b.dataset.mode;render();document.querySelector(`[data-mode="${state.mode}"]`).focus({preventScroll:true});return;}
 if(b.dataset.piece){if(record().completed||state.order.includes(b.dataset.piece))return;state.order.push(b.dataset.piece);state.feedback=null;save();render();document.querySelector(state.order.length===3?'[data-action="check"]':'.piece-pool button:not(:disabled)')?.focus({preventScroll:true});return;}
 if(b.dataset.remove!==undefined){if(record().completed)return;state.order.splice(Number(b.dataset.remove),1);state.feedback=null;save();render();document.querySelector('.piece-pool button:not(:disabled)')?.focus({preventScroll:true});return;}
 switch(b.dataset.action){
 case 'start':newGame();break;
 case 'resume':state=saved;state.muted=true;tickAt=Date.now();render('.question-title');break;
 case 'sound':state.muted=!state.muted;play();if(state.screen!=='start')save();render();document.querySelector('[data-action="sound"]').focus({preventScroll:true});break;
 case 'teacher':teacher();break;
 case 'close-teacher':document.querySelector('#teacher').close();document.querySelector('[data-action="teacher"]').focus();break;
 case 'check':check();break;
 case 'hint':E.support(state,record(),'hint');save();render('.hint-panel');break;
 case 'clear':state.order=[];state.feedback=null;save();render();document.querySelector('.piece-pool button:not(:disabled)')?.focus({preventScroll:true});break;
 case 'model':E.support(state,record(),'model');record().completed=true;state.feedback={correct:false,title:'Let’s learn this one together.',text:q().explanation};save();render('#feedback');break;
 case 'next':if(!record().completed)return;if(state.index===pack().length-1){state.screen='finish';audio.pause();celebrate();}else {state.index++;state.order=[];state.answer=null;state.proof=null;state.feedback=null;}save();render(state.screen==='finish'?'.results h1':'.question-title');break;
 case 'pause':save();saved=JSON.parse(JSON.stringify(state));state.screen='start';audio.pause();render('h1');break;
 case 'home':saved=JSON.parse(JSON.stringify(state));state.screen='start';audio.pause();render('h1');break;
 case 'export':exportCSV();break;
 case 'print':window.print();break;
 }
 });
 document.addEventListener('visibilitychange',()=>{tickAt=Date.now();if(document.hidden){audio.pause();if(state.screen!=='start')save();}else if(state.screen==='game')play();});
 setInterval(()=>{const now=Date.now();if(state.screen==='game'&&!document.hidden&&!document.querySelector('#teacher')?.open){state.seconds+=Math.min(2,Math.round((now-tickAt)/1000));const el=document.querySelector('#timer');if(el)el.textContent=clock();if(state.seconds%10===0)save();}tickAt=now;},1000);
 window.addEventListener('pagehide',()=>{if(state.screen!=='start')save();});
 render();
})();
